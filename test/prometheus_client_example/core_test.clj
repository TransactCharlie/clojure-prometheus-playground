(ns prometheus-client-example.core-test
  (:require [clojure.test :refer :all]
            [prometheus-client-example.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
